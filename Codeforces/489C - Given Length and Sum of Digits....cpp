#include <iostream>

using namespace std;

#define endl '\n'
#define fast  ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

int main()
{
    fast;
    int m, s; cin >> m >> s;
    if( s == 0 ) {
		if( l == 1 ) 
			cout "0 0\n";
		else
			cout "-1 -1\n"; 			
	}
    string min_num = "", max_num = "";
    for( int i = 0; i < m; i++ ) {
    	int d = min( 9, s )
    }
    return 0;
}
