#include <iostream>
#include <math.h>
 
 
#define yo ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
 
using namespace std;
 
 
int main()
{
    yo;
    int a, b, wa = 0, wb = 0, d = 0;
    cin >> a >> b; 
    wa = a; 
    wb = 6 - b + 1; 
    if( (b - a) % 2 == 0){
    	wa += (b - a) / 2;
    	wb += (b - a) / 2;
    }
    else d
    

}